info:
  name: Delete Category (Leaf Only)
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{base_url}}/api/categories/{{leafCategoryId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
