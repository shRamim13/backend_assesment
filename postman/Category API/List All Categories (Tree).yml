info:
  name: List All Categories (Tree)
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
