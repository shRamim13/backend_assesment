info:
  name: Deactivate Category (Cascade)
  type: http
  seq: 10

http:
  method: PATCH
  url: "{{base_url}}/api/categories/{{categoryId}}/deactivate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
