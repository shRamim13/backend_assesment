info:
  name: Get Category By ID
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/categories/{{categoryId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
