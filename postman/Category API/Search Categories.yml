info:
  name: Search Categories
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/categories/search?q=watch"
  params:
    - name: q
      value: watch
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
