opencollection: 1.0.0

info:
  name: Category API

request:
  variables:
    - name: base_url
      value: http://localhost:3000

docs:
  content: CRUD for hierarchical categories with unlimited nesting, Redis caching, cascade deactivation.
  type: text/markdown
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
